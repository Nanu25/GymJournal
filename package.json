{
  "name": "gymjournal",
  "version": "1.0.0",
  "description": "Gym Journal application",
  "scripts": {
    "build": "cd backend && npm install && npm run build && cd ../my-app && npm install && npm run build",
    "start": "cd backend && npm start"
  },
  "engines": {
    "node": "18.x"
  },
  "cacheDirectories": ["backend/node_modules", "my-app/node_modules"]
}
