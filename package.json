{
  "name": "gymjournal",
  "version": "1.0.0",
  "description": "Gym Journal Application",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "start": "cd backend && node dist/app.js",
    "build": "echo 'Using pre-compiled JavaScript'",
    "heroku-postbuild": "cd backend && npm install --production"
  },
  "cacheDirectories": ["backend/node_modules"]
}
