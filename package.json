{
  "name": "gymjournal",
  "version": "1.0.0",
  "description": "Gym Journal Application",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "start": "cd backend && node dist/app.js",
    "build": "echo 'Using pre-compiled JavaScript'",
    "heroku-postbuild": "cd backend && npm install && cd ../my-app && npm install && npm run build"
  },
  "cacheDirectories": ["backend/node_modules", "my-app/node_modules"]
}
